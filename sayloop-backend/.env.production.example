# Production API server (https://sayloop.ddns.net) — copy to .env on the host

NODE_ENV=production
PORT=4000

FRONTEND_URL=https://sayloop-henna.vercel.app
FRONTEND_URLS=https://sayloop-henna.vercel.app,http://sayloop-henna.vercel.app

# Required for session review (Whisper + GPT coaching)
OPENAI_API_KEY=sk-...

# ... DATABASE_URL, CLERK_SECRET_KEY, GEMINI_API_KEY, OPENAI_API_KEY
