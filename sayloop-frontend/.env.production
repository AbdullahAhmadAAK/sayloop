# Used automatically by `npm run build` (Vercel production deploy)
# API host only — do not add /api (the app appends it)

# Point at your AWS API (same host for REST + Socket.IO). Update if using a different domain.
VITE_API_URL=https://sayloop.ddns.net
VITE_SOCKET_URL=https://sayloop.ddns.net

# Set in Vercel dashboard if not committed:
# VITE_CLERK_PUBLISHABLE_KEY=pk_live_...
